# jmx-test
How to run
* install openjdk
`sudo apt update && sudo apt install -y openjdk-17-jre`
*  install [gradle-8.5](https://gradle.org/)

```
gradle build
gradle run
```
or
```
gradle build
gradle shadowJar
./run.sh
```
How to stop
```
Ctrl-c
```
